# 보안 정책 (Security Policy)

> English: this project is maintained in Korean. Security reports in English are
> welcome and will be handled the same way. See "취약점 보고" below for the
> reporting channel.

이 저장소는 **연합(federation) 모델**로 배포된다. 중앙 서버도, 멀티테넌트 호스팅도,
공용 계정도 없다. 각 설치는 그것을 돌리는 연구자 본인이 소유하고 운영한다. 그래서
보안 정책도 두 층으로 나뉜다.

| 층 | 책임자 | 이 문서의 범위 |
|---|---|---|
| 업스트림 코드 (이 저장소) | 업스트림 유지보수자 | **여기서 다룬다** |
| 개별 설치의 운영 (자격증명·노드·데이터) | 그 설치의 소유자 | 다루지 않는다 |
| 그룹 관리형 스킬 저장소·서명키 | 각 그룹 관리자 | 다루지 않는다 |

---

## 취약점 보고

**공개 이슈로 올리지 마세요.** 수정본이 배포되기 전에 공개되면 모든 설치가 동시에
노출된다.

- **보고처**: <orientpine@gmail.com>
- GitHub Private Vulnerability Reporting을 사용할 수 있으면 그 경로도 동등하게
  유효하다(공개 저장소 생성 시 활성화 대상).

### 보고에 담아주면 좋은 것

1. 영향받는 구성요소(경로 또는 모듈명)와 확인한 버전(릴리스 태그 또는 커밋 SHA)
2. 재현 절차 — 가능하면 최소 재현
3. 관측한 영향(무엇을 우회하거나 읽거나 실행할 수 있는가)
4. 토큰·실제 메일 본문·개인 노트 원문은 **마스킹**해서 보낸다. 재현에 필요한
   최소한만 보내면 충분하다.

### 응답 약속

이 프로젝트는 **개인 유지보수자 1인**이 운영한다. 24/7 대응 조직이 없으므로
합의되지 않은 SLA를 약속하지 않는다. 대신 다음을 **best-effort**로 지킨다.

| 단계 | 목표 |
|---|---|
| 접수 확인 | 영업일 기준 5일 이내 |
| 초기 판정(영향·심각도) | 접수 확인 후 10일 이내 |
| 수정 릴리스 또는 완화 안내 | 심각도에 따라 조정, 진행 상황을 보고자에게 계속 공유 |

목표를 못 지킬 상황이면 침묵하지 않고 그 사실을 알린다. 조율된 공개(coordinated
disclosure)를 기본으로 하며, 공개 시점은 보고자와 합의한다.

### 이 프로젝트가 특히 관심 있는 취약점

이 저장소의 중심 불변식은 **"소유자가 승인하지 않은 외부효과는 일어나지 않는다"**
이다. 따라서 다음은 심각도가 높다.

- 승인 게이트 우회 — 소유자의 ✅ 없이 메일 발송·캘린더 등록·문서 쓰기·스킬 마운트가
  일어나는 경로
- 승인 바인딩 파괴 — 승인 후 내용이 바뀌었는데도 실행되는 경로(action hash 우회)
- 공급망 — 서명 검증 우회, 신뢰 키 파일(`/etc/autophagy/*-allowed-signers`)을
  에이전트 계정이 쓸 수 있게 되는 경로
- 권한 경계 — `agent`/`peer`/`ops` 계정 분리나 sudoers 제약을 넘는 경로
- 신원 위조 — roster에 없는 발신자의 보고·봉투가 처리되는 경로
- 비밀 유출 — 토큰·개인키·원문 로그가 Discord·로그·저장소로 새는 경로

반대로 **설계상 그런 것**이라 취약점이 아닌 것도 있다. 보고 전에 확인하면 서로
시간을 아낀다.

- **그룹 관리자는 이미 마운트된 스킬을 원격 회수할 수 없다.** 이는 "각자 자기 설치를
  소유한다"의 직접적 귀결이며 영구 설계다(README「연합 모델」참조).
- **관리형 스킬은 자동 전파되지만 자동 활성화되지 않는다.** live 마운트에 설치
  소유자의 ✅를 요구하는 것은 의도된 제약이다.
- **설치 소유자 본인이 자기 노드에서 할 수 있는 일**은 권한 상승이 아니다.
- 실패 시 동작을 멈추는 fail-closed 거동(설정 누락·서명 실패·모호한 판단)은
  가용성 결함으로 보고할 수는 있으나 보안 취약점이 아니다.

---

## 지원 범위

- **지원되는 버전은 최신 릴리스 태그 하나뿐이다.** 이전 릴리스로의 백포트는 하지
  않는다. 유지보수자가 1인이고, 노드가 서명된 최신 릴리스로 자동 수렴하도록
  설계되어 있어 장기 지원 브랜치를 유지할 근거가 없다.
- 보안 수정은 **다음 릴리스 태그**로 나간다. 각 설치의 리컨실러가 그 태그를 검증하고
  수렴한다.
- 릴리스 버전과 설정·roster·매니페스트 **스키마 버전**의 관계, 그리고 런타임 상태
  파일이 바뀌었을 때의 거동은 [`docs/guide/versioning-support.md`](docs/guide/versioning-support.md)가
  소유한다.
- 업데이트 신뢰키의 **보관·회전 절차**와 나쁜 릴리스 대응은 플랫폼 운영자 매뉴얼
  [`docs/guide/manual-maintainer.md`](docs/guide/manual-maintainer.md)이 소유한다. 이 문서는 그
  절차를 복사하지 않는다.

### 지원하지 않는 것

- 개별 설치의 운영 대행·원격 진단. 각 설치는 그 소유자의 것이고, 유지보수자는
  타인의 노드·자격증명·데이터에 접근하지 않는다.
- 그룹 관리자의 스킬 저장소나 서명키 운영. 그것은 그 그룹 관리자의 책임이다.
- 개인·기관 데이터의 백업이나 복구.
- 포크·개조본. 업스트림 코드가 아닌 것에서 발견된 문제는 그 포크에 보고한다.

---

## 무보증 고지

이 소프트웨어는 [Apache License 2.0](LICENSE)으로 배포된다. 보증 부인과 책임 제한은
**라이선스 본문 7조(Disclaimer of Warranty)·8조(Limitation of Liability)가 그대로
적용**되며, 이 문서는 그것을 다시 쓰지 않는다. 요약하면 소프트웨어는 "AS IS"로
제공되고 어떤 종류의 보증도 없다.

특히 이 시스템은 메일 발송·일정 등록·문서 저장·과제비 같은 **실제 외부효과**를
다룬다. 승인 게이트와 fail-closed 설계는 사고 가능성을 줄이기 위한 것이지 사고가
없다는 보장이 아니다. 각 설치의 소유자가 자기 설치에서 일어나는 일에 대한 최종
책임자다.

---

## 저장소 두 개에 대한 참고

개발 origin(private)과 공개 배포본은 서로 다른 저장소이며, 동기화는
`automation/public_export.sh` 단일 경로로만 이루어진다. 보안 보고와 릴리스는
**공개 저장소**를 기준으로 처리한다.

공개 저장소는 **`orientpine/cytoplasm`**이며 2026-08-15 `v1.0.0`으로 생성됐다. 개발은
앞으로도 private 저장소에서만 이뤄지며, 공개 저장소에는 아무도 직접 push하지
않는다.
