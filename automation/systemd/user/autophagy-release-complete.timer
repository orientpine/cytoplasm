# Workstation-only: the signing key lives here; never install this unit on a node.
[Unit]
Description=Check for approved Autophagy releases every two minutes

[Timer]
OnBootSec=2min
OnUnitActiveSec=2min
AccuracySec=30s
Persistent=false

[Install]
WantedBy=timers.target
