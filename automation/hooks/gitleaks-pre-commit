#!/usr/bin/env bash
set -euo pipefail

command -v gitleaks >/dev/null 2>&1 || {
  printf '[pre-commit] REFUSED — gitleaks is not installed or not on PATH.\n' >&2
  exit 1
}
exec gitleaks git --pre-commit --staged --redact --verbose
